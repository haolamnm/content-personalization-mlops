---
title: Decision Log
id: agents-decision-log
date: 2026-08-25
type: decision-log
status: active
tags: [mlops, decisions]
related:
  - ../README.md
  - ./index.json
---

# Decision Log

Append-only; newest at bottom. Corrections append a superseding row — history is never rewritten. Architectural decisions graduate into `docs/adr/` with a row here pointing at them. Machine-readable catalog of docs: [`index.json`](./index.json) (generated).

| Date | Decision |
|------|----------|
| 2026-08-24 | Theme picked: **content personalization** over IoT prediction ([ADR 0001](../adr/0001-content-personalization-theme.md)) |
| 2026-08-24 | Repo layout: `.notes/` + `.repos/` gitignored local-only; root AGENTS.md is the tracked index ([ADR 0002](../adr/0002-local-learning-and-reference-layout.md)) |
| 2026-08-24 | Reference clone registered: `purchase-prediction-mlops` @ `8c86edd` (bmd1905/Customer-Purchase-Prediction-ML-System) — Phase 0 study subject |
| 2026-08-25 | Machine constraint recorded: M-series Mac, 16 GB unified memory — compose profile groups, one at a time (`AGENTS.local.md`, [ADR 0003](../adr/0003-ram-budgeted-local-infrastructure.md)) |
| 2026-08-25 | Reference metadata is structural `.repos/metadata.json` generated by `scripts/gen_repos_metadata.py` + curated `scripts/repos_registry.json`; handcrafted markdown dropped |
| 2026-08-25 | Polyglot stack locked: Java 25 (Flink/Kafka Streams), Python 3.14 (ML plane), Go 1.26 (gateway/BFF/simulators), Rust 1.98 (retrieval hot path), TS+SvelteKit (FE) ([ADR 0004](../adr/0004-polyglot-language-per-concern.md)) |
| 2026-08-25 | FE framework: SvelteKit (Svelte 5) over Next.js — optimization over familiarity ([ADR 0005](../adr/0005-sveltekit-over-nextjs.md)) |
| 2026-08-25 | Context layout settled: root holds only `CONTEXT-MAP.md`; `CONTEXT.md` files live beside each module's code under `platform/`; session state lives in `.worklog/FOCUS.md` (local-only) |
| 2026-08-25 | Doc indexes are generated JSON (`docs/index.json`, `docs/adr/index.json`, `docs/agents/index.json`) via `scripts/gen_docs_metadata.py`; frontmatter in each md is the source; no hand-maintained markdown index tables |
| 2026-08-25 | Machine-facts layer centralized: root `COMPUTER.md` (developer-local, gitignored); rules and ADRs reference it instead of hardcoding specs |
| 2026-08-25 | Container runtime corrected to **OrbStack** (not Docker Desktop); VM memory is dynamic — explicit cap TBD before heavy groups |
| 2026-08-25 | Toolchains installed via Homebrew: go 1.27.0 ([ADR 0004](../adr/0004-polyglot-language-per-concern.md) pin bumped 1.26→1.27), openjdk@25 25.0.4.1, python@3.14 3.14.7; GOPATH moved to XDG `~/.local/share/go`, GOBIN → existing `~/.local/bin` |
| 2026-08-25 | Node 24 LTS active via volta shims (`~/.local/share/volta`); brew node 26 stays installed but shadowed |
| 2026-08-25 | Java build tool: **Maven** over Gradle — no persistent daemon RAM against [ADR 0003](../adr/0003-ram-budgeted-local-infrastructure.md), Maven-first Flink/Kafka docs |
| 2026-08-25 | JS toolchain: **Bun** primary, **pnpm** fallback, npm banned; Node 24 remains the server runtime ([ADR 0005](../adr/0005-sveltekit-over-nextjs.md) amended) |
| 2026-08-25 | Agent search tool fixed to ripgrep (`rg`), never grep — recorded in `AGENTS.local.md` §5 and `COMPUTER.md` |
| 2026-08-25 | Skills layer completed: Tribal craft set ported (grilling, wait-what, domain-modeling, codebase-design, implement, tdd, deslopify, codebase-review, improve-codebase-architecture, experiment, maintain-worklog, handoff, resolving-merge-conflicts); `study-reference`→`studying-mlops` and `run-local-stack`→`managing-mlops-services` renamed; `yag` skill not ported (documents their own binary) |
| 2026-08-25 | Python quality toolchain locked: **ruff** (lint+format), **ty** + **basedpyright** (type checks) — no mypy, no black ([python-envs-and-quality](../../.agents/rules/python-envs-and-quality.md)) |
| 2026-08-25 | data images pinned (registry-verified, not from memory): `postgres:18.6`, `mongo:8.0.29`, `apache/kafka:4.3.1` (KRaft-only), `minio/minio:RELEASE.2025-09-07T16-13-09Z` |
| 2026-08-25 | MongoDB pinned to 8.0.x despite 8.3.8 being current stable — Debezium 3.7 certifies Mongo 6.0/7.0/8.0 only; CDC wiring is the next branch and must not start blocked |
| 2026-08-25 | MinIO community edition archived upstream (repo read-only since 2026-04-25; final security release never published as an image tag) — pinned to last pullable tag `RELEASE.2025-09-07T16-13-09Z`; S3-compatible alternative (AIStor is commercial) revisited only if a phase demands newer MinIO |
| 2026-08-25 | Profile groups renamed to one-word identifiers per owner decision: `core-data` → **`data`** (file `compose.data.yaml`, containers `data-*`, Make targets `data-*`, branch re-cut to `feat/bootstrap-data-stack`); `obs-light`/`obs-heavy` kept hyphenated — the qualifier encodes distinct RAM budgets |
| 2026-08-25 | Review corrections: Debezium dropped from data-group membership in ADR 0003 + skill tables (it belongs to the future cdc slice; estimate rescaled ~3–4 → ~2.5–3.5 GB); Postgres volume mounted at `/var/lib/postgresql` — postgres:18 moved PGDATA to `/var/lib/postgresql/18/docker`, legacy-path mounts break or silently fail to persist |
| 2026-08-25 | Machine-facts layer split per computer: root `COMPUTER.md` monolith → `.computers/{MACBOOK,THINKBOOK,WORKSTATION}.md` (gitignored as a folder); convention: durable facts recorded as values, moving facts recorded as the command that yields them; WORKSTATION (GCP VM, CloudThinker) documented with an explicit out-of-scope wall — never a deploy target for this repo |
| 2026-08-25 | No-PII-in-tracked-git policy adopted per owner instruction: tracked files carry no personal names, emails, private/tailnet IPs, MACs, or account handles — such facts live only in gitignored `.computers/`, `.worklog/`, local SSH config. Explicit exception to append-only: the attribution in this log's rename row was redacted in place (decision content untouched, nothing restored) as a one-time privacy scrub |
| 2026-08-25 | THINKBOOK adopted as standing remote deployment target ([ADR 0006](../adr/0006-thinkbook-remote-deployment-target.md)): Tailscale transport, push-from-Mac deploy model, per-box `.env` secrets, host ports overridable via env (ThinkBook coexists with system postgresql via 15432); WORKSTATION scope wall restated |
| 2026-08-25 | CDC slice live end-to-end on THINKBOOK: Debezium Connect pinned `quay.io/debezium/connect:3.6.1.Final` (newest stable; 3.7 still alpha upstream) joining data group's `mlops-data` network; Postgres `wal_level=logical`; insert→Kafka envelope proven. Kafka listener scheme corrected: in-mesh `kafka:9092`, host-only `localhost:29094` — host listener must never share the in-network port or metadata redirects break cross-container clients |
| 2026-08-25 | Event gateway live end-to-end on THINKBOOK: first owned Go service (`platform/services/event-gateway`, franz-go behind a Producer port) producing `mlops.events.raw` keyed by user_id; explicit `topics-ensure` pattern adopted because Kafka 4.x auto-create is disabled; Makefile guard whitelists the sanctioned trio (data+cdc+gateway) |
| 2026-08-25 | Flink adoption pins: Flink **2.2.1** + `flink-connector-kafka` **5.0.0-2.2** (matched pair — connector releases track specific Flink minors and no `-2.3` build existed; Kafka clients ride transitively at 4.2.x, compatible with broker 4.3.1). Connector 5.x commits offsets on checkpoints by design; checkpoint storage moved to `CheckpointingOptions.CHECKPOINT_STORAGE=filesystem` + `CHECKPOINTS_DIRECTORY` URI (old string setter gone) |
| 2026-08-25 | **Known constraint**: embedded `LocalStreamEnvironment` at parallelism>1 never fires event-time windows on Flink 2.2.1 — records arrive and upstream watermarks emit, but no timer triggers; reproduced in-process (`Parallel3ReproTest`, kept disabled as repro recipe). Embedded runs pinned to parallelism 1 until a real cluster lands via deployment ADR. Hardening kept regardless: punctuated per-record watermarks + `withIdleness(2s)` against idle-partition stalls |
